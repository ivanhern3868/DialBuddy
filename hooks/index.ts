/**
 * Custom Hooks Index
 *
 * Business Purpose:
 * Central export point for all custom hooks in the application.
 * Simplifies imports throughout the codebase.
 *
 * Usage:
 * ```tsx
 * import { useContacts, useHaptics } from '@/hooks';
 * ```
 */

export { useContacts } from './useContacts';
export { useHaptics } from './useHaptics';

// Add future hooks here:
// export { useProgress } from './useProgress';
// export { useVoice } from './useVoice';
// export { useEmergency } from './useEmergency';
